#include <Tiger-stdlib-stack-data.hera>

CBON()
    INC(SP, 3)
    SET(R1, 10)
    STORE(R1, 1, FP)
    SET(R1, 2)
    STORE(R1, 2, FP)
    MOVE(R12, SP)
    INC(SP, 5)
    LOAD(R1, 1, FP)
    STORE(R1, 3, R12)
    LOAD(R1, 2, FP)
    STORE(R1, 4, R12)
    STORE(FP, 2, R12)
    CALL(R12, div)
    LOAD(R1, 3, R12)
    DEC(SP, 5)
    STORE(R1, 0, FP)
    MOVE(R12, SP)
    INC(SP, 4)
    LOAD(R1, 0, FP)
    STORE(R1, 3, R12)
    STORE(FP, 2, R12)
    CALL(R12, printint)
    LOAD(R1, 3, R12)
    DEC(SP, 4)
HALT()

#include <Tiger-stdlib-stack.hera>
